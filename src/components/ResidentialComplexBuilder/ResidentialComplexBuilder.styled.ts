import { styled } from '@mui/system';

export const BuildingDataContainer = styled('div')(({ theme }) => ({
  display: 'flex',
  gap: theme.spacing(2),
  marginTop: theme.spacing(2),
  alignItems: 'baseline',
}));
